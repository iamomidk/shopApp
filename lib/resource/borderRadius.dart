import 'package:flutter/material.dart';

// all border radius in this page 

// you can access very easy

// use for textfield
BorderRadius borderRadius_5 = BorderRadius.all(Radius.circular(5.0));
// use for image of product
BorderRadius borderRadius_15 = BorderRadius.all(Radius.circular(15.0));
// use for buttons
BorderRadius borderRadius_30 = BorderRadius.all(Radius.circular(30.0));
